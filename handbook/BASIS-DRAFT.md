# `basis` — moved

The draft is finished and implemented. **The criteria now live in
MODEL-SPEC §3.1**, which is the only copy to edit.

- the eight values, their tests, and the arguments behind them — MODEL-SPEC §3.1
- `searched`, `rival`, `refuted`, `sharesWarrantWith` — same section
- validation — `tools/check-model.js`, beside the category check
- the remaining category-vocabulary work the design turned up — ROADMAP §7
- the draft assignment the field was built from — `tmp/basis-v7.json`

Implemented 2026-09-15: all 143 curated entities carry a `basis`.
