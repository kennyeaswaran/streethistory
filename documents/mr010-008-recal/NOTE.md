# mr010-008-recal — scratch copy for re-alignment (2026-09-10)

A throwaway duplicate of `documents/mr010-008/` holding only the render and a
seeded alignment. **No `.js`, no streets bundle, no TASK.md**, so it shows up
in the map tool under "needs aligning and coverage" and nothing here reaches the
model. Delete the whole folder when done.

**Scale and rotation are already set — only drag.** Both were derived from the
sheet itself, not from any assumed street correspondence:

sheet says Courses magnetic Var 14 deg 15 min E, so a magnetic-north-up drawing puts up-the-page at 14.25deg true; scale left alone

|  | before | now |
|---|---|---|
| metres per pixel | 0.3204 | 0.3204 |
| up-the-page bearing | 18.26 deg | 14.25 deg |

The image centre is pinned to the same ground point it had before, so the sheet
opens near where you left it; translation is the only thing still resting on a
correspondence.
