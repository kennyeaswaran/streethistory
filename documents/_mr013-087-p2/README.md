# _mr013-087-p2 — parked

Sheet 2 of 2 of the Mills and Wicks Extension plat (M.R. 13-87). The leading
underscore is the parking convention: present on disk, ignored by
`documents/index.js` and therefore by the checker and the generator.

Parked because Kenny read it (2026-08-30) and found only one street on it that
was ever built — not enough to be worth aligning. Nothing has been thrown away:
the render is here, and the full two-page PDF is in `documents/mr013-087-p1/`.

To pick it up later: rename this folder to `mr013-087-p2`, copy the PDF in,
and align the render as its own document. Sheet 1 is the model to follow.
